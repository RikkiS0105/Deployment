Activate environment -----> conda activate <env variable>.
Install Libraries ------> pip install -r requirements.txt
Install Flask ------> pip install flask
Flask<dir> - app.py, readme.md, templates<dir>
